import { useWorkoutContext } from "../useWorkoutPlanContext";

export const useFindKey = () => {
  const { selectedPlan, customWorkoutPlan, currentStepIndex } =
    useWorkoutContext();

  function findKey() {
    let key = "";
  }
};
