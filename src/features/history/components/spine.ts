/**
 * Geometry for the ledger spine — the single vertical rule every entry and
 * month marker hangs off. All three pieces have to draw the rule at the same
 * x or the line breaks, so the numbers live here rather than in each sheet.
 */
export const SPINE_GUTTER = 36;
export const SPINE_X = 13;
